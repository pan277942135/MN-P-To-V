name: M2 Stage Acceptance Record

# One-shot acceptance completed on 2026-08-17 for frozen source:
# 46da7e8e6c90442ab7236b4f850bb5956a689bec
#
# The live UAT deployment is zaojing-m2-uat revision zaojing-m2-uat-00015-qvk.
# This workflow is intentionally inert after successful acceptance to prevent accidental redeploys.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  acceptance-record:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'M2 stage acceptance already completed; see Actions run 31998365543.'

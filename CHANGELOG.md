# 版本变更日志 (CHANGELOG)

## 🎬 V2.0 Cinema (v2.0.0-v2.0-cinema)
**发布时间**: 2026-08-05 19:00:00 (UTC-7)  
**版本标识**: `V2.0 Cinema` / `v2.0.0-v2.0-cinema`  

### 🚀 核心优化与更新
1. **任务记录双向自动同步**: 
   - 补全后端 `/api/videos/list` 列表查询接口，按 `x-connection-id` 隔离与同步任务。
   - 点击“生成”跳转至【任务记录】页面后，会自动触发列表轮询刷新，彻底解决切换页面后历史为空的问题。
2. **失败原因精准推演与一键重试**:
   - 当云端渲染失败或超时，完整展现技术推演诊断日志与精准错误描述（如 API 额度超限、授权失效或长轮询中断）。
   - 在历史卡片与详情弹窗中新增 **【一键重试】** 按钮，无需重新上传场景图或配置角色，直接恢复原始参数发起二次生成。
3. **版本号管理机制**:
   - 顶部导航栏版本标识升级为 **V2.0 Cinema**，并支持发布迭代自动增量递增。

---

# M1.2 版本归档记录

**归档版本**: `v1.2.0-m1.2-archived`  
**归档时间**: 2026-08-05 18:40:00 (UTC-7)  
**构建标识**: `m1.2-archived` / `v1.2-stable`  

---

## 📌 M1.2 版本核心演进与里程碑

### 1. 视频渲染与并发处理优化
- **服务端任务幂等防重提交**: 在 `/api/videos/generate` 接口新增 `idempotencyKey` 检查，拦截 60 秒内相同场景模式与图片的重复请求，防止并发多次扣费与重复生成。
- **任务存储全局同步**: 统一使用全局 `serverVideoTaskStore` 管理长生命周期渲染任务，并支持基于 `x-connection-id` 跨会话/跨页面的任务轮询与状态恢复。
- **任务防重触发 UI 保护**: 优化工作室界面 (`StudioPage`)，在任务处于提交或执行状态时再次点击生成按钮，自动拦截并向用户弹出温馨提示，同时平滑转入【任务记录】页面查看进度。

### 2. 多账号与凭据管理 (Multi-Account & Credentials)
- **多账号持久化方案**: 重构 `CredentialService` 与 IndexedDB，支持添加、切换、删除多个 Google Vertex AI / Service Account 凭据。
- **会话状态感知与共享**: 新增 `x-connection-id` 统一请求头机制，多标签页及历史记录页能实时感知主账号连接状态，保持渲染进度一致。

### 3. 错误诊断与日志去敏系统 (Diagnosis & Privacy)
- **极简日志与安全脱敏**: 引入 `redactSecrets` 工具，全面拦截 API Key、OAuth Token 及隐私路径，保障离线诊断日志安全。
- **一键诊断控制台 Modal**: 提供完整包含 `buildVersion`、`commitHash` 及底层 HTTP / Vertex 错误推演日志的诊断视图。

### 4. 架构与界面稳定性 (Stability & UI/UX)
- **高对比度黑金风主题**: 精致的 Tailwind 暗黑风格，包含高亮微光感动画与流畅的侧边导航。
- **零配置多端兼容**: 支持 AI Studio 开发环境与 Cloud Run 生产环境自动部署模式。

---

*造景工作站 (Zaojing Workbench) M1.2 版本已成功归档。*
